title: A — B
short_title: A — B
description: Andrew Boardman
baseurl:
permalink: "/:path/"
defaults:
  - scope:
      path: "_page"
    values:
    layout: page
  - scope:
       path: ""
       type: posts
       values:
           layout: post
collections:
  posts:
    output: true
  projects:
    output: true
    permalink: "/projects/:path/"
# tag_page_layout: tag_page
# tag_page_dir: tag
jekyll-archives:
  enabled: all
  layout: "archive"
gems:
  - jekyll-menus
  - jekyll-feed
  - jekyll-archives
